# credentials.yml

# REST channel (default, works for API/web integrations)
rest:

# Rasa built-in shell (for testing locally)
console:

# Facebook Messenger integration (optional)
#facebook:
#  verify: "your-verify-token"
#  secret: "your-app-secret"
#  page-access-token: "your-page-access-token"

# Telegram integration (optional)
#telegram:
#  access_token: "your-telegram-bot-token"
#  verify: "your-bot-username"
#  webhook_url: "https://your-server.com/webhooks/telegram/webhook"

# Socket.io channel (for custom web chat widgets on a website)
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# Twilio (optional, for WhatsApp/SMS)
#twilio:
#  account_sid: "your-account-sid"
#  auth_token: "your-auth-token"
#  twilio_number: "your-twilio-number"
